name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Test
    runs-on: ubuntu-24.04
    outputs:
      release_tag: ${{ steps.release.outputs.tag }}

    steps:
      - name: Check out opsctl
        uses: actions/checkout@v6
        with:
          path: opsctl
          persist-credentials: false

      - name: Set up private dependency
        uses: ./opsctl/.github/actions/setup-release
        with:
          opsd-rust-token: ${{ secrets.OPSD_RUST_TOKEN }}

      - name: Select release tag
        id: release
        working-directory: opsctl
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            tag="$GITHUB_REF_NAME"
          else
            version="$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[] | select(.name == "opsctl") | .version')"
            tag="v${version}"
          fi
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"

      - name: Test
        working-directory: opsctl
        run: cargo test --locked

  build:
    name: Build ${{ matrix.target }}
    needs: test
    runs-on: ${{ matrix.runner }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            runner: macos-15
            install_musl: false
          - target: aarch64-unknown-linux-musl
            runner: ubuntu-24.04-arm
            install_musl: true
          - target: x86_64-apple-darwin
            runner: macos-15-intel
            install_musl: false
          - target: x86_64-unknown-linux-musl
            runner: ubuntu-24.04
            install_musl: true

    steps:
      - name: Check out opsctl
        uses: actions/checkout@v6
        with:
          path: opsctl
          persist-credentials: false

      - name: Set up release build
        uses: ./opsctl/.github/actions/setup-release
        with:
          opsd-rust-token: ${{ secrets.OPSD_RUST_TOKEN }}
          install-dist: "true"

      - name: Install musl toolchain
        if: ${{ matrix.install_musl }}
        run: sudo apt-get update && sudo apt-get install --yes musl-tools

      - name: Configure musl compiler
        if: ${{ matrix.install_musl }}
        env:
          TARGET: ${{ matrix.target }}
        run: |
          target_key="${TARGET//-/_}"
          echo "CC_${target_key}=musl-gcc" >> "$GITHUB_ENV"
          echo "CARGO_TARGET_${target_key^^}_LINKER=musl-gcc" >> "$GITHUB_ENV"

      - name: Install Rust target
        run: rustup target add "${{ matrix.target }}"

      - name: Build release archive
        working-directory: opsctl
        run: |
          dist build \
            --artifacts=local \
            --target="${{ matrix.target }}" \
            --tag="${{ needs.test.outputs.release_tag }}" \
            --output-format=json > dist-manifest.json
          cp dist-manifest.json \
            "target/distrib/${{ matrix.target }}-dist-manifest.json"

      - name: Upload release archive
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.target }}
          path: |
            opsctl/target/distrib/opsctl-${{ matrix.target }}.tar.xz
            opsctl/target/distrib/opsctl-${{ matrix.target }}.tar.xz.sha256
            opsctl/target/distrib/${{ matrix.target }}-dist-manifest.json
          if-no-files-found: error

  package:
    name: Package and publish
    needs: [test, build]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Check out opsctl
        uses: actions/checkout@v6
        with:
          path: opsctl
          persist-credentials: false

      - name: Set up release build
        uses: ./opsctl/.github/actions/setup-release
        with:
          opsd-rust-token: ${{ secrets.OPSD_RUST_TOKEN }}
          install-dist: "true"

      - name: Download release archives
        uses: actions/download-artifact@v5
        with:
          pattern: release-*
          path: opsctl/target/distrib
          merge-multiple: true

      - name: Build installer and unified checksum
        working-directory: opsctl
        run: |
          dist build \
            --artifacts=global \
            --tag="${{ needs.test.outputs.release_tag }}" \
            --output-format=json > dist-manifest.json

      - name: Upload complete release
        uses: actions/upload-artifact@v4
        with:
          name: release
          path: |
            opsctl/target/distrib/*.tar.xz
            opsctl/target/distrib/*.tar.xz.sha256
            opsctl/target/distrib/opsctl-installer.sh
            opsctl/target/distrib/sha256.sum
          if-no-files-found: error

      - name: Configure AWS credentials
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        uses: aws-actions/configure-aws-credentials@v6.1.0
        with:
          aws-region: eu-west-2
          role-to-assume: arn:aws:iam::091833700814:role/github-opsctl-release
          role-session-name: github-actions-opsctl-release

      - name: Read release destination
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        id: destination
        run: |
          bucket="$(aws ssm get-parameter \
            --name /opsd/opsctl/release/bucket_name \
            --query Parameter.Value \
            --output text)"
          distribution="$(aws ssm get-parameter \
            --name /opsd/opsctl/release/cloudfront_distribution_id \
            --query Parameter.Value \
            --output text)"
          echo "bucket=${bucket}" >> "$GITHUB_OUTPUT"
          echo "distribution=${distribution}" >> "$GITHUB_OUTPUT"

      - name: Publish release
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        env:
          BUCKET: ${{ steps.destination.outputs.bucket }}
          RELEASE_TAG: ${{ needs.test.outputs.release_tag }}
        run: |
          aws s3 cp opsctl/target/distrib \
            "s3://${BUCKET}/opsctl/releases/${RELEASE_TAG}/" \
            --recursive \
            --exclude "*" \
            --include "*.tar.xz" \
            --include "*.tar.xz.sha256" \
            --include "opsctl-installer.sh" \
            --include "sha256.sum" \
            --cache-control "public,max-age=31536000,immutable"

          aws s3 cp opsctl/target/distrib/opsctl-installer.sh \
            "s3://${BUCKET}/opsctl/install.sh" \
            --cache-control "public,max-age=300" \
            --content-type "text/x-shellscript"

          jq -n --arg tag "$RELEASE_TAG" '{tag: $tag}' > latest.json
          aws s3 cp latest.json "s3://${BUCKET}/opsctl/latest.json" \
            --cache-control "public,max-age=300" \
            --content-type "application/json"

      - name: Invalidate mutable downloads
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        run: |
          aws cloudfront create-invalidation \
            --distribution-id "${{ steps.destination.outputs.distribution }}" \
            --paths /opsctl/install.sh /opsctl/latest.json
